/*
 *  Define context's for redux actions.  Defining a context will allow for an action to be
 *  used in multiple places within the same user context.
 */

export const CONTEXT = {
    //  **** load reports actions context's ****
    LOAD_REPORTS: {
        NAV: 'LOAD_REPORTS_NAV'
    }
};
