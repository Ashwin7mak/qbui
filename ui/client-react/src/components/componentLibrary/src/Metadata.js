import QBPanelMetadata from 'component-metadata!../../QBPanel/qbpanel.js';
import QBIconMetadata from 'component-metadata!../../qbIcon/qbicon.js';
import _ from 'lodash';

var Metadata = _.merge(QBPanelMetadata, QBIconMetadata);

export default Metadata;
